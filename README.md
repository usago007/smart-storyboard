# FatMug — Smart Storyboard

AI-powered storyboard generator. Runs in the browser — no backend, no API keys.

```bash
npm install && npm run dev
```

Deployed at [usago007.github.io/smart-storyboard](https://usago007.github.io/smart-storyboard).

MIT
